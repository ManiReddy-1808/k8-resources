apiVersion: v1
kind: Pod
metadata:
  name: resources
spec:
  containers:
  - name: nginx
    image: nginx
  resources:
    requests: # Soft Limits
      memory: "64Mi" #RAM
      cpu: "100m" #CPU

    limits: # Hard Limits
      memory: "128Mi"
      cpu: "150m"